[build-system]
requires = [
    "setuptools>=64",
    "wheel",
    "pybind11",
    "torch==2.13.0",
    "torchvision==0.21.0"
]
build-backend = "setuptools.build_meta"

[project]
name = "Voxelium"
dynamic = ["version"]
description = "Voxelium - a powerful tool for volumetric CryoEM processing."
readme = "README.md"
requires-python = ">=3.9,<3.13"
authors = [
  { name = "Dari Kimanius", email = "dari.kimanius@biohub.org" }
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]
keywords = ["CryoEM", "CryoET", "Reconstruction", "ML"]
license = { text = "MIT" }

dependencies = [
    "torch==2.13.0",
    "torchvision==0.21.0",
    "loguru",
    "matplotlib",
    "mrcfile",
    "numpy==1.*",
    "scikit-learn",
    "scipy",
    "tensorboard",
    "alive_progress",
]

[tool.setuptools]
license-files = []
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["voxelium", "voxelium.*"]

[tool.setuptools.dynamic]
version = { attr = "voxelium.__version__" }

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "src/voxelium/torch_extensions/sparse3d",
    "src/voxelium/torch_extensions/inplace_topk"
]
